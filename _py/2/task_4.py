# Завдання 4
# Напишіть програму, в якій користувач вводить фразу з клавіатури, яка складається з 10 символів. На екрані виведіть
# суму ASCII-кодів символів введеного рядка.
st = input('Введiть фразу з 10 символiв: ')
s = ord(st[0]) + ord(st[1]) + ord(st[2]) + ord(st[3]) + ord(st[4]) + ord(st[5]) + \
    ord(st[6]) + ord(st[7]) + ord(st[8]) + ord(st[9])
print('Сума ASCII-кодів символів введеного рядка =', s)